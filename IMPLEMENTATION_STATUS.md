# ゲーム実装状況

更新日: 2026-08-03

## 判定基準

- **実装済み**: 現在のゲーム内で一連の処理が動作するもの
- **一部実装**: 基礎処理はあるが、当初予定していた効果や他システムとの接続が不足しているもの
- **未実装**: ファイルが空、または対応する処理がまだ存在しないもの

## 実装済み

### シーン管理

- Title → Game → GameClear / GameOver → Title のシーン遷移
- Title、Game、GameClear、GameOverの各シーン名をImGuiで表示
- `G`キーでTitleからGameへ移行
- GameClear / GameOverから`G`キーでTitleへ戻る
- プレイヤーHPが0になった場合のGameOver移行
- デバッグ用の`1`キーによるGameClear移行
- デバッグ用の`2`キーによるGameOver移行

### プレイヤー

- 仮モデルとして`cube.obj`を描画
- WASDによる移動
- コントローラー左スティックによるアナログ移動とデッドゾーン処理
- 移動方向に合わせたY軸回転
- 最短角度を使った回転補間
- 正面方向へのダッシュ
- キーボードの左Shift、コントローラーのAボタンからダッシュ可能
- ダッシュ後の補間減速
- ダッシュ減速後の移動スロー
- ダッシュクールタイム
- HP、ダメージ、無敵時間
- ノックバックとスタン
- 円形障害物からのXZ平面押し出し

### カメラ

- プレイヤーを中心とした約45度の俯瞰カメラ
- プレイヤー座標へのカメラ補間
- 移動速度と加速度を利用した進行方向の先読み
- マウスホイールによるズームイン・ズームアウト
- 最小／最大ズーム距離の制限
- デバッグカメラ切り替え

### ステージ

- `Ground.obj`の読み込みと描画
- `Wall.obj`の読み込みと描画
- WorldTransformの共通更新処理

### 当たり判定

- Sphere同士
- AABB同士
- SphereとAABB
- PointとSphere / AABB
- SegmentとSphere / AABB
- OBB同士
- AABBとOBB
- SphereとOBB
- PointとOBB
- SegmentとOBB
- WorldTransformからのOBB生成
- XZ平面の円同士、点と円、線分と円

### 補間・イージング

- float、Vector2、Vector3のLerp
- 角度の最短補間
- Linear
- Sine系
- Quad系
- Cubic系
- SmoothStep / SmootherStep
- EaseOutBack
- EaseOutBounce

### ボス共通

- `Boss.obj`の読み込みと描画
- 固定順による攻撃切り替え
- 待機中は白、予兆中は黄色、攻撃中は赤へモデル色を変更
- 攻撃間クールタイム

### 隙間付き円形弾幕

- `cube.obj`を仮弾モデルとして使用
- ボスを中心とした円形弾幕
- 回避用の隙間
- 隙間位置の変化
- 複数ウェーブ
- 弾の移動、寿命、プレイヤーへのダメージ

### 地面槍攻撃

- `PredictionCircle.obj`による黄色い攻撃予測円
- 予測円を5%から最大サイズまで`SmootherStep`で拡大
- プレイヤーの位置を狙って連続出現
- 槍の本数を`kSpearCount`から変更可能
- `EaseOutBack`による勢いのある上昇
- 上昇中に当たったプレイヤーへダメージ、ノックバック、スタン
- 上昇後は一定時間、円形の壁としてプレイヤーを押し戻す
- 残存時間中はダメージなし
- 槍が残っている間も次のボス攻撃へ移行
- 消滅時に地面へ勢いよく引っ込む

## 一部実装

### ボス周辺爆破

実装済み:

- ボス周辺への黄色い攻撃予測円
- 予測円を5%から最大サイズまで`SmootherStep`で拡大
- 爆発中は赤色へ変更
- 範囲内のプレイヤーへダメージ

不足:

- 命中時にプレイヤーへ付与するスロー効果
- 爆発専用のモデル、パーティクル、SE

### Mob大量召喚

実装済み:

- `Mob.obj`を20体召喚
- ボスを中心とした内側・外側の二重円配置
- 各出現位置へ黄色い予測円を表示
- 予測円を5%から最大サイズまで`SmootherStep`で拡大
- 予兆終了後にMobを一斉出現
- プレイヤー追尾と向きの補間
- 接触ダメージ
- Mob同士の円形押し出しによる重なり防止
- MobごとのHP
- 円形範囲からMobへダメージを与えるための関数
- 制限時間と生存数の記録

不足:

- プレイヤー攻撃からMobのダメージ関数への接続
- MobのHP表示や被弾演出
- 制限時間内に倒せなかった場合のボスHP回復
- 制限時間内に倒せなかった場合のボス必殺技チャージ
- Mobとステージ壁、地面槍などの障害物との衝突
- Mobの出現位置のランダム化

### DDA／ボス行動制御

実装済み:

- 攻撃の状態に応じたボスの色変更
- 各攻撃を管理する`BossAttackManager`

不足:

- 現在は固定順であり、プレイヤーのHP、距離、行動、戦闘時間などに応じた動的な難易度調整は未実装
- 攻撃選択の重み、連続使用制限、フェーズごとの行動テーブル

### ステータス効果

実装済み:

- プレイヤークラス内のスタン
- ノックバック
- ダッシュ後スロー

不足:

- `StatusEffectManager`は空クラスのため、効果の一元管理は未実装
- 複数スローの上書き／加算ルール
- 毒、炎上、バフ、デバフなどの汎用効果
- UIへの残り時間表示

### ボスHPとゲームクリア

- ボスにはHP変数`bossHP_ = 500`が存在する
- プレイヤー攻撃、被ダメージ、死亡判定には未接続
- 現在のGameClearはデバッグ用の`1`キーのみで発生

## 未実装

### ボス攻撃

- プレイヤーへ向かう突進攻撃
- 突進命中時のノックバックとスロー
- 回転しながら撃つ極大レーザー
- レーザー後のボス短時間ダウン
- 地面槍による突進／レーザーの遮蔽判定
- Mob未撃破数に応じたボス回復または必殺技チャージ

`BossChargeAttack.h/.cpp`と`BossRotatingLaserAttack.h/.cpp`は現在空ファイル。

### プレイヤー攻撃・スキル

- 通常攻撃
- メインスキル5個
- サブスキル5個
- メインスキルとサブスキルの組み合わせ処理
- 攻撃の生成、更新、描画、当たり判定を統括する仕組み
- ボスとMobへの実戦用ダメージ処理
- スキル選択UI、クールタイムUI

### JSONデータ管理

- ダメージ
- 継続時間
- 分裂数
- 弾速、範囲、クールタイムなどの外部設定
- JSONの読み込み、検証、デフォルト値、リロード

現在の攻撃パラメータは各C++ヘッダー内の`constexpr`として管理されている。

### ゲーム進行・演出

- ボス撃破によるGameClear
- ボスHPバー
- プレイヤーの正式モデル
- 攻撃エフェクト、パーティクル、画面振動
- BGM、SE
- タイトル、クリア、ゲームオーバーの正式画面
- ポーズ、リトライ、設定画面

## 現在のボス攻撃順

1. 隙間付き円形弾幕
2. ボス周辺爆破
3. 地面槍
4. Mob大量召喚
5. 1へ戻る

地面槍は残存中でも次の攻撃へ進み、障害物として残る。

## 次に進める推奨順

1. プレイヤー通常攻撃と共通の攻撃基底／管理クラスを作る
2. ボスとMobへダメージを接続し、ボス撃破でGameClearへ移行する
3. Mob時間切れ時のボス回復または必殺技チャージを実装する
4. 突進攻撃を実装する
5. 回転レーザーと槍による遮蔽を実装する
6. `StatusEffectManager`へスロー、スタン、ノックバックを整理する
7. スキルパラメータをJSON化する
8. メイン／サブスキルの組み合わせシステムを実装する

## ビルド状況

- 構成: Debug x64
- 直近確認: ビルド成功
- 警告: 0
- エラー: 0
